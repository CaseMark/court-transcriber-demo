# =============================================================================
# Court Transcriber Demo - Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values:
#   cp .env.example .env.local

# -----------------------------------------------------------------------------
# Case.dev API (Required for transcription and OCR)
# Get your API key from: https://console.case.dev
# -----------------------------------------------------------------------------
CASE_API_KEY=sk_case_your_api_key_here

# -----------------------------------------------------------------------------
# Vercel Blob Storage (Production)
# Used for storing files that Case.dev APIs need to fetch.
# Auto-configured when you add Vercel Blob to your project:
#   vercel link && vercel blob
# -----------------------------------------------------------------------------
# BLOB_READ_WRITE_TOKEN=vercel_blob_rw_...

# -----------------------------------------------------------------------------
# Cloudflared Tunnel (Local Development Only)
# Case.dev APIs need to fetch files from a public URL.
# In production, Vercel Blob provides direct URLs.
#
# For local dev:
#   1. Install: brew install cloudflared
#   2. Run: cloudflared tunnel --url http://localhost:3000
#   3. Copy the https URL below
# -----------------------------------------------------------------------------
CLOUD_URL=https://your-tunnel-url.trycloudflare.com

# -----------------------------------------------------------------------------
# Database (Optional - app uses local storage by default)
# -----------------------------------------------------------------------------
# DATABASE_URL=postgres://user:password@host:5432/database

# -----------------------------------------------------------------------------
# Authentication (Optional - local auth enabled by default)
# -----------------------------------------------------------------------------
# NEXTAUTH_URL=http://localhost:3000
# NEXTAUTH_SECRET=your-secret-here